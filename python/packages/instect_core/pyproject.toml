[project]
name = "instect-core"
version = "0.1.0a0"
description = "Core Python types and shared utilities for the Insect harness."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/instect_core"]
