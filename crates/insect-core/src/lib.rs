//! `insect-core` — shared domain types for the Insect harness.
//!
//! This crate will host the budget tree, trial supervisor, and the public
//! re-exports of the generated `insect.eval.v1` schema types. Stubbed for now.

pub fn placeholder() {}
