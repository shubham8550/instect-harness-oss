//! `insect-proto` — tonic-generated gRPC client/server stubs.
//!
//! Build script (added in a later wave) will run `tonic-build` against the
//! `proto/insect/v1/*.proto` files at the repo root. Stubbed for now.

pub fn placeholder() {}
